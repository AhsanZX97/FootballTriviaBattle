# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Android signing secrets — never commit
keystore.properties
*.jks
*.keystore

# Google service account key + the env file used to push Supabase function
# secrets. These grant access to the Play Developer API — never commit them.
supabase/.env.secrets
*-service-account*.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

docs/

src/assets/screenshots

# Playwright MCP session artifacts (snapshots, console logs, screenshots)
.playwright-mcp/
