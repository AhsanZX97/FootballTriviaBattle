# Production build: point the app at the deployed Render WebSocket server.
# Vite bakes this in at build time. Used by the native release build
# (npm run build -> cap sync) and the Vercel web build; Vercel also sets the
# same value in its dashboard, so the two agree.
VITE_WS_URL=wss://football-trivia-ws.onrender.com
